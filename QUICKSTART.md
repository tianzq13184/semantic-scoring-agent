# 快速启动指南

## 🚀 启动步骤

### 1. 检查环境

确保已安装 Python 3.8+ 和所需依赖：

```bash
# 检查 Python 版本
python --version

# 安装依赖（如果还没安装）
pip install -r requirements.txt
```

### 2. 配置环境变量

创建 `.env` 文件（如果还没有）：

```bash
# 在项目根目录创建 .env 文件
cat > .env << EOF
# LLM 配置（必需）
OPENAI_API_KEY=your_api_key_here

# 模型配置（可选，默认 gpt-4o-mini）
MODEL_ID=gpt-4o-mini

# 数据库配置（可选，默认 SQLite）
DB_URL=sqlite:///./answer_eval.db

# API 基础 URL（UI 使用）
API_BASE=http://127.0.0.1:8000
EOF
```

**重要**：将 `your_api_key_here` 替换为你的实际 OpenAI API Key。

### 3. 初始化数据库（首次运行）

```bash
python run_migrations.py
```

这将创建数据库表并导入初始数据。

### 4. 启动后端 API

打开**第一个终端窗口**，在**项目根目录**运行：

```bash
# 在项目根目录（semantic-scoring-agent）运行
uvicorn api.main:app --reload --port 8000
```

**注意**：不要进入 `api` 目录，直接在项目根目录运行即可。

看到以下输出表示启动成功：
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Application startup complete.
```

API 文档地址：http://127.0.0.1:8000/docs

### 5. 启动前端 UI

打开**第二个终端窗口**：

```bash
cd ui
streamlit run app.py
```

浏览器会自动打开，或手动访问：http://localhost:8501

## 📝 使用说明

### 访问界面

1. **评估答案页面**（默认）
   - 选择题目（从数据库动态加载）
   - 输入学生答案
   - 点击"Evaluate"进行评估

2. **评估结果列表页面**
   - 查看所有评估结果
   - 支持筛选和分页
   - 点击"查看详情"查看详细信息

3. **评估详情页面**
   - 查看评估的详细信息
   - 教师可以覆盖评分并添加备注

### API 测试

访问 http://127.0.0.1:8000/docs 可以：
- 查看所有 API 接口
- 在线测试 API
- 查看请求/响应格式

## 🔧 常见问题

### 1. 端口被占用

如果 8000 或 8501 端口被占用，可以修改：

**后端**：
```bash
uvicorn main:app --reload --port 8001
```

**前端**：
```bash
streamlit run app.py --server.port 8502
```

然后更新 `.env` 中的 `API_BASE` 为对应端口。

### 2. 数据库错误

如果遇到数据库相关错误：

```bash
# 删除旧数据库（注意：会丢失数据）
rm answer_eval.db

# 重新初始化
python run_migrations.py
```

### 3. API Key 错误

确保 `.env` 文件中的 `OPENAI_API_KEY` 已正确设置。

### 4. 依赖缺失

如果遇到导入错误：

```bash
pip install -r requirements.txt
```

## 📊 验证安装

运行测试验证系统是否正常：

```bash
# 运行核心功能测试
pytest tests/test_models/ tests/test_db/ tests/test_services/ -v
```

预期结果：`38 passed`

## 🎯 下一步

- 查看 [README.md](README.md) 了解完整功能
- 查看 [docs/PRD.md](docs/PRD.md) 了解产品需求
- 查看 API 文档：http://127.0.0.1:8000/docs

---

**提示**：保持两个终端窗口都运行，一个运行后端，一个运行前端。

